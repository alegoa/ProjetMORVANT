name: Concatener les indices
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: cat /tree/main/indices/indices* > indices.md
   
      - run: echo "🍏 This job's status is ${{ job.status }}."

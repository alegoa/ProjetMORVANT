

name: Concatener les indices
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name : copier
    - run: cat indices/indice* > in.md
    - name : ech
    - run: echo indices/indice3



name: Concatener les indices
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - run: cat ../../../indices/indices* > indices.md
   
    - run: echo "🍏 This job's status is ${{ job.status }}."
